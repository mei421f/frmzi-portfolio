{
  "name": "frmzi-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": { "node": ">=18" }
}
